<?xml version="1.0" encoding="utf-8" ?>
<toolkit2:Popup
    x:Class="ForestalCasablancaApp.Popups.TrozoAserrableSummaryPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:ForestalCasablancaApp.Models"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:toolkit2="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
    xmlns:viewModels="clr-namespace:ForestalCasablancaApp.ViewModels"
    x:DataType="viewModels:TrozoAserrableViewModel"
    Color="Transparent">


    <Border Style="{StaticResource Popup}">

        <ScrollView>
            <VerticalStackLayout
                Margin="{OnIdiom Phone='8,0,0,0'}"
                Padding="{OnIdiom Phone=10}"
                Spacing="8">

                <Image
                    Aspect="AspectFill"
                    HeightRequest="{OnIdiom Phone=80}"
                    Source="popup_icon.png"
                    WidthRequest="{OnIdiom Phone=80}">

                    <Image.Shadow>
                        <Shadow Offset="20,20" />
                    </Image.Shadow>
                </Image>

                <Label
                    Style="{StaticResource PopupTitle}"
                    Text="Resumen" />
                <BoxView Style="{StaticResource Separator}" />

                <!--  Datos del Cliente  -->
                <Label
                    Style="{StaticResource PopupSectionLabel}"
                    Text="Datos del Cliente:" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding Cliente.Nombre, StringFormat='Nombre: {0}'}" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding Cliente.RUT, StringFormat='RUT: {0}'}" />
                <BoxView Style="{StaticResource Separator}" />

                <!--  Datos del Camión  -->
                <Label
                    Style="{StaticResource PopupSectionLabel}"
                    Text="Datos del Camión:" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding DatosCamion.UnidadOrigen, StringFormat='Unidad de Origen: {0}'}" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding DatosCamion.EmpresaTransportista, StringFormat='Empresa: {0}'}" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding DatosCamion.Chofer, StringFormat='Chofer: {0}'}" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding DatosCamion.RutChofer, StringFormat='RUT Chofer: {0}'}" />
                <Label Style="{StaticResource PopupContent}" Text="{Binding DatosCamion.Patente, StringFormat='Patente: {0}'}" />
                <BoxView Style="{StaticResource Separator}" />

                <!--  Especie 1  -->
                <Label
                    Style="{StaticResource PopupSectionLabel}"
                    Text="Especie 1:" />
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding EspecieUno, StringFormat='Especie: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding LargoEspecieUno, StringFormat='Largo: {0}'}" />
                </Grid>
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding TotalSumLista1, StringFormat='Cantidad: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding FinalTotalSumLista1, StringFormat='Volumen: {0} m3'}" />
                </Grid>
                <BoxView Style="{StaticResource Separator}" />

                <!--  Especie 2  -->
                <Label
                    Style="{StaticResource PopupSectionLabel}"
                    Text="Especie 2:" />
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding EspecieDos, StringFormat='Especie: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding LargoEspecieDos, StringFormat='Largo: {0}'}" />
                </Grid>
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding TotalSumLista2, StringFormat='Cantidad: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding FinalTotalSumLista2, StringFormat='Volumen: {0} m3'}" />
                </Grid>
                <BoxView Style="{StaticResource Separator}" />

                <!--  Especie 3  -->
                <Label
                    Style="{StaticResource PopupSectionLabel}"
                    Text="Especie 3:" />
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding EspecieTres, StringFormat='Especie: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding LargoEspecieTres, StringFormat='Largo: {0}'}" />
                </Grid>
                <Grid ColumnDefinitions="*, *">
                    <Label
                        Grid.Column="0"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding TotalSumLista3, StringFormat='Cantidad: {0}'}" />
                    <Label
                        Grid.Column="1"
                        Style="{StaticResource PopupContent}"
                        Text="{Binding FinalTotalSumLista3, StringFormat='Volumen: {0} m3'}" />
                </Grid>
                <BoxView Style="{StaticResource Separator}" />

                <Grid ColumnDefinitions="*, *">
                    <Button
                        Grid.Column="0"
                        Command="{Binding GeneratePDFCommand}"
                        Style="{StaticResource Buttons}"
                        Text="Generar PDF" />
                    <Button
                        Grid.Column="1"
                        Command="{Binding ClosePopupCommand}"
                        Style="{StaticResource Buttons}"
                        Text="Volver" />
                </Grid>
            </VerticalStackLayout>
        </ScrollView>
    </Border>
</toolkit2:Popup>